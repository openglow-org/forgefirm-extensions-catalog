<!-- One version of one package per pull request.
     What it takes: https://docs.forgefirm.org/developers/extension-listing/ -->

- Package id:
- Version:
- The source's tag, which the archive is built from:
- What it does, in plain words, and what it sends off the machine:

- [ ] `packages/<id>/<version>.json`, as `ffx index record` writes it from the signed archive
- [ ] `packages/<id>/key.pub` with the first version of a package, and unchanged since
- [ ] The source is published under a license that lets anyone read, build, and change it
